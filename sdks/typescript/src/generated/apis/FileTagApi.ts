/* tslint:disable */
/* eslint-disable */
/**
 * Gemina API
 * Extract structured data from invoices and financial documents, then search, aggregate, and chat over everything you\'ve processed. Authenticate with your API key in the `X-API-Key` header; browser-embedded components use short-lived session tokens minted via `POST /v1/sessions/token`. Guides: https://console.gemina.co/docs
 *
 * The version of the OpenAPI document: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import * as runtime from '../runtime';
import {
    type CreditTransactionListOutDTO,
    CreditTransactionListOutDTOFromJSON,
    CreditTransactionListOutDTOToJSON,
} from '../models/CreditTransactionListOutDTO';
import {
    type FileTagBalanceOutDTO,
    FileTagBalanceOutDTOFromJSON,
    FileTagBalanceOutDTOToJSON,
} from '../models/FileTagBalanceOutDTO';
import {
    type FileTagResultOutDTO,
    FileTagResultOutDTOFromJSON,
    FileTagResultOutDTOToJSON,
} from '../models/FileTagResultOutDTO';
import {
    type HTTPValidationError,
    HTTPValidationErrorFromJSON,
    HTTPValidationErrorToJSON,
} from '../models/HTTPValidationError';

export interface GetMyFiletagCreditHistoryRequest {
    skip?: number;
    limit?: number;
}

export interface TagDocumentRequest {
    file: Blob;
}

export interface TagDocumentByUserRequest {
    file: Blob;
}

/**
 * 
 */
export class FileTagApi extends runtime.BaseAPI {

    /**
     * Creates request options for getMyFiletagBalance without sending the request
     */
    async getMyFiletagBalanceRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            headerParameters["Authorization"] = await this.configuration.accessToken("OAuth2PasswordBearer", []);
        }


        let urlPath = `/api/v1/filetag/me/balance`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Current user\'s FileTag balance, effective monthly allowance, and next reset timestamp. Used by the console dashboard to surface remaining tags.
     * Get My Filetag Balance
     */
    async getMyFiletagBalanceRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FileTagBalanceOutDTO>> {
        const requestOptions = await this.getMyFiletagBalanceRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FileTagBalanceOutDTOFromJSON(jsonValue));
    }

    /**
     * Current user\'s FileTag balance, effective monthly allowance, and next reset timestamp. Used by the console dashboard to surface remaining tags.
     * Get My Filetag Balance
     */
    async getMyFiletagBalance(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FileTagBalanceOutDTO> {
        const response = await this.getMyFiletagBalanceRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getMyFiletagCreditHistory without sending the request
     */
    async getMyFiletagCreditHistoryRequestOpts(requestParameters: GetMyFiletagCreditHistoryRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['skip'] != null) {
            queryParameters['skip'] = requestParameters['skip'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            headerParameters["Authorization"] = await this.configuration.accessToken("OAuth2PasswordBearer", []);
        }


        let urlPath = `/api/v1/filetag/me/credits`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Paginated audit trail of FileTag credit transactions for the current user. Returns the same rows as ``GET /v1/subscriptions/me/credits?meter=filetag_credits`` — kept as a FileTag-namespaced alias so free-tier users (who have no subscription endpoint affinity) can discover the history endpoint naturally.
     * Get My Filetag Credit History
     */
    async getMyFiletagCreditHistoryRaw(requestParameters: GetMyFiletagCreditHistoryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CreditTransactionListOutDTO>> {
        const requestOptions = await this.getMyFiletagCreditHistoryRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => CreditTransactionListOutDTOFromJSON(jsonValue));
    }

    /**
     * Paginated audit trail of FileTag credit transactions for the current user. Returns the same rows as ``GET /v1/subscriptions/me/credits?meter=filetag_credits`` — kept as a FileTag-namespaced alias so free-tier users (who have no subscription endpoint affinity) can discover the history endpoint naturally.
     * Get My Filetag Credit History
     */
    async getMyFiletagCreditHistory(requestParameters: GetMyFiletagCreditHistoryRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CreditTransactionListOutDTO> {
        const response = await this.getMyFiletagCreditHistoryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for tagDocument without sending the request
     */
    async tagDocumentRequestOpts(requestParameters: TagDocumentRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['file'] == null) {
            throw new runtime.RequiredError(
                'file',
                'Required parameter "file" was null or undefined when calling tagDocument().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-Key"] = await this.configuration.apiKey("X-API-Key"); // APIKeyHeader authentication
        }

        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        // use FormData to transmit files using content-type "multipart/form-data"
        useForm = canConsumeForm;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['file'] != null) {
            formParams.append('file', requestParameters['file'] as any);
        }


        let urlPath = `/api/v1/filetag`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * Extract filing metadata (title, type, date, tags, ...) from a single PDF or image and return a suggested filename plus a set of filename patterns.  Requires API-key auth (``X-API-Key`` header). Subject to the per-API-key burst rate limit and -- for free-tier users -- the monthly FileTag credit allowance.
     * Tag Document
     */
    async tagDocumentRaw(requestParameters: TagDocumentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FileTagResultOutDTO>> {
        const requestOptions = await this.tagDocumentRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FileTagResultOutDTOFromJSON(jsonValue));
    }

    /**
     * Extract filing metadata (title, type, date, tags, ...) from a single PDF or image and return a suggested filename plus a set of filename patterns.  Requires API-key auth (``X-API-Key`` header). Subject to the per-API-key burst rate limit and -- for free-tier users -- the monthly FileTag credit allowance.
     * Tag Document
     */
    async tagDocument(requestParameters: TagDocumentRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FileTagResultOutDTO> {
        const response = await this.tagDocumentRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for tagDocumentByUser without sending the request
     */
    async tagDocumentByUserRequestOpts(requestParameters: TagDocumentByUserRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['file'] == null) {
            throw new runtime.RequiredError(
                'file',
                'Required parameter "file" was null or undefined when calling tagDocumentByUser().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-API-Key"] = await this.configuration.apiKey("X-API-Key"); // APIKeyHeader authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            // oauth required
            headerParameters["Authorization"] = await this.configuration.accessToken("OAuth2PasswordBearer", []);
        }

        const consumes: runtime.Consume[] = [
            { contentType: 'multipart/form-data' },
        ];
        // @ts-ignore: canConsumeForm may be unused
        const canConsumeForm = runtime.canConsumeForm(consumes);

        let formParams: { append(param: string, value: any): any };
        let useForm = false;
        // use FormData to transmit files using content-type "multipart/form-data"
        useForm = canConsumeForm;
        if (useForm) {
            formParams = new FormData();
        } else {
            formParams = new URLSearchParams();
        }

        if (requestParameters['file'] != null) {
            formParams.append('file', requestParameters['file'] as any);
        }


        let urlPath = `/api/v1/filetag/user`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: formParams,
        };
    }

    /**
     * Browser-safe variant of ``POST /v1/filetag``.  Authenticates with the caller\'s JWT (``Authorization: Bearer <jwt>``) and treats the ``X-API-Key`` header as the *UUID id* of one of the user\'s API keys — NOT the raw secret. The key\'s ownership is verified server-side by matching ``APIKeyModel.id == header AND user_id == jwt``, so the raw secret never needs to leave the server.  Mirrors the auth pattern used by ``POST /v1/documents/uploads/user``. For server-to-server callers (the raw-key path used by MCP and public SDKs), continue to use ``POST /v1/filetag``.
     * Tag Document By User
     */
    async tagDocumentByUserRaw(requestParameters: TagDocumentByUserRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FileTagResultOutDTO>> {
        const requestOptions = await this.tagDocumentByUserRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FileTagResultOutDTOFromJSON(jsonValue));
    }

    /**
     * Browser-safe variant of ``POST /v1/filetag``.  Authenticates with the caller\'s JWT (``Authorization: Bearer <jwt>``) and treats the ``X-API-Key`` header as the *UUID id* of one of the user\'s API keys — NOT the raw secret. The key\'s ownership is verified server-side by matching ``APIKeyModel.id == header AND user_id == jwt``, so the raw secret never needs to leave the server.  Mirrors the auth pattern used by ``POST /v1/documents/uploads/user``. For server-to-server callers (the raw-key path used by MCP and public SDKs), continue to use ``POST /v1/filetag``.
     * Tag Document By User
     */
    async tagDocumentByUser(requestParameters: TagDocumentByUserRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FileTagResultOutDTO> {
        const response = await this.tagDocumentByUserRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
