/* tslint:disable */
/* eslint-disable */
/**
 * Gemina API
 * Extract structured data from invoices and financial documents, then search, aggregate, and chat over everything you\'ve processed. Authenticate with your API key in the `X-API-Key` header; browser-embedded components use short-lived session tokens minted via `POST /v1/sessions/token`. Guides: https://console.gemina.co/docs
 *
 * The version of the OpenAPI document: 1.3.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


/**
 * Whether an extraction's cost is final, still coming, or never charged.
 * 
 * Exists because a bare ``null`` cost is ambiguous across three genuinely
 * different situations, and a client reconciling spend has to tell them
 * apart:
 * 
 * - ``pending`` -- the charge has not been written yet. Credits are deducted
 *   in the Celery post phase, which runs *after* the extraction result is
 *   available, so a freshly finished extraction legitimately has no cost on
 *   record. Retry.
 * - ``settled`` -- a charge is on record. This is the authoritative number.
 * - ``not_charged`` -- terminal: no charge exists and none is coming. Either
 *   the extraction FAILED (billing follows outcome, so a failure is never
 *   charged), or billing ran and did not charge -- usually a real billing
 *   failure, since ``post_summary_update_task`` swallows ``SubscriptionError``
 *   to a log line. Never resolves on its own, so a client must not poll it.
 * 
 * Two caveats on ``not_charged``, both from ``post_tasks_complete`` being a
 * coarse proxy for "billing finished":
 * 
 * 1. It records that ``post_summary_update_task`` *started*, not that it
 *    charged -- the flag is written as that task's first statement, well
 *    before the charge itself. A read landing in between sees
 *    ``not_charged`` for an extraction that is about to be billed.
 * 2. It is per-correlation (per document), so on a multi-extraction document
 *    the first extraction to reach billing flips it for its siblings too.
 * 
 * Both windows are short (tens of milliseconds), but they mean ``not_charged``
 * is advisory: alert on it only after a grace period, never treat a single
 * reading as proof of an unbilled extraction.
 * @export
 */
export const ExtractionCostStateModel = {
    Pending: 'pending',
    Settled: 'settled',
    NotCharged: 'not_charged'
} as const;
export type ExtractionCostStateModel = typeof ExtractionCostStateModel[keyof typeof ExtractionCostStateModel];


export function instanceOfExtractionCostStateModel(value: any): boolean {
    for (const key in ExtractionCostStateModel) {
        if (Object.prototype.hasOwnProperty.call(ExtractionCostStateModel, key)) {
            if (ExtractionCostStateModel[key as keyof typeof ExtractionCostStateModel] === value) {
                return true;
            }
        }
    }
    return false;
}

export function ExtractionCostStateModelFromJSON(json: any): ExtractionCostStateModel {
    return ExtractionCostStateModelFromJSONTyped(json, false);
}

export function ExtractionCostStateModelFromJSONTyped(json: any, ignoreDiscriminator: boolean): ExtractionCostStateModel {
    return json as ExtractionCostStateModel;
}

export function ExtractionCostStateModelToJSON(value?: ExtractionCostStateModel | null): any {
    return value as any;
}

export function ExtractionCostStateModelToJSONTyped(value: any, ignoreDiscriminator: boolean): ExtractionCostStateModel {
    return value as ExtractionCostStateModel;
}

