/**
 * Package version — the single hand-written source of truth (mirrors
 * package.json). Note: unlike the server-side Gemina SDKs, the TypeScript
 * client sends NO custom User-Agent header (browsers forbid overriding it).
 * VERSION is exported for consumers and diagnostics only.
 */
export const VERSION = '0.1.2';
