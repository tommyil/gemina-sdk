# coding: utf-8

"""
    Gemina API

    Extract structured data from invoices and financial documents, then search, aggregate, and chat over everything you've processed. Authenticate with your API key in the `X-API-Key` header; browser-embedded components use short-lived session tokens minted via `POST /v1/sessions/token`. Guides: https://console.gemina.co/docs

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List
from uuid import UUID
from gemina.generated.models.file_tag_filename_patterns_out_dto import FileTagFilenamePatternsOutDTO
from gemina.generated.models.file_tag_metadata_out_dto import FileTagMetadataOutDTO
from gemina.generated.models.next_action_out_dto import NextActionOutDTO
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class FileTagResultOutDTO(BaseModel):
    """
    Final FileTagService result -- returned by the REST router and the MCP tool.
    """ # noqa: E501
    document_extraction_id: UUID
    document_id: UUID
    enriched_file_expires_at: datetime = Field(description="Absolute UTC timestamp when ``enriched_file_url`` stops resolving.")
    enriched_file_expires_in_seconds: StrictInt = Field(description="Seconds until ``enriched_file_url`` expires, captured at response time. Easier for agents to reason about than the absolute timestamp.")
    enriched_file_url: StrictStr = Field(description="Short-lived signed URL to the input file with the extracted metadata embedded directly into the document properties (XMP for PDFs, EXIF/XMP for images). This is the canonical enriched output of the tool -- the file the user actually wants. Agents should download it within ``enriched_file_expires_in_seconds`` and save it under ``suggested_filename`` rather than embedding metadata client-side. Skip the download only if the user explicitly asked for the metadata payload alone.")
    filename_patterns: FileTagFilenamePatternsOutDTO
    metadata: FileTagMetadataOutDTO
    next_action: NextActionOutDTO = Field(description="Structured copy-paste recipe for the post-tag follow-up: HTTP GET ``enriched_file_url`` and save as ``suggested_filename``. Mirrors the ``next_tool_call`` pattern from ``files_create_upload`` -- one structured instruction per step keeps multi-step flows reliable across volatile agent scratchpads.")
    suggested_filename: StrictStr = Field(description="Recommended filename for the enriched file -- one of the six ``filename_patterns``, picked as the safest default for general use. Pair this with ``enriched_file_url`` when saving the downloaded file.")
    __properties: ClassVar[List[str]] = ["document_extraction_id", "document_id", "enriched_file_expires_at", "enriched_file_expires_in_seconds", "enriched_file_url", "filename_patterns", "metadata", "next_action", "suggested_filename"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of FileTagResultOutDTO from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of filename_patterns
        if self.filename_patterns:
            _dict['filename_patterns'] = self.filename_patterns.to_dict()
        # override the default output from pydantic by calling `to_dict()` of metadata
        if self.metadata:
            _dict['metadata'] = self.metadata.to_dict()
        # override the default output from pydantic by calling `to_dict()` of next_action
        if self.next_action:
            _dict['next_action'] = self.next_action.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of FileTagResultOutDTO from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "document_extraction_id": obj.get("document_extraction_id"),
            "document_id": obj.get("document_id"),
            "enriched_file_expires_at": obj.get("enriched_file_expires_at"),
            "enriched_file_expires_in_seconds": obj.get("enriched_file_expires_in_seconds"),
            "enriched_file_url": obj.get("enriched_file_url"),
            "filename_patterns": FileTagFilenamePatternsOutDTO.from_dict(obj["filename_patterns"]) if obj.get("filename_patterns") is not None else None,
            "metadata": FileTagMetadataOutDTO.from_dict(obj["metadata"]) if obj.get("metadata") is not None else None,
            "next_action": NextActionOutDTO.from_dict(obj["next_action"]) if obj.get("next_action") is not None else None,
            "suggested_filename": obj.get("suggested_filename")
        })
        return _obj


