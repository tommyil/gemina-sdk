# coding: utf-8

"""
    Gemina API

    Extract structured data from invoices and financial documents, then search, aggregate, and chat over everything you've processed. Authenticate with your API key in the `X-API-Key` header; browser-embedded components use short-lived session tokens minted via `POST /v1/sessions/token`. Guides: https://console.gemina.co/docs

    The version of the OpenAPI document: 1.0.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class NextActionOutDTO(BaseModel):
    """
    Structured follow-up recipe for an agent capability (not an MCP tool).  Sister pattern to :class:`app.components.files.dtos.FilesNextToolCallOutDTO`: that one names a specific MCP tool by literal name (``tag_file``); this one names a generic *capability* (``http_get_and_save``) the agent maps to whatever local mechanism it has -- ``WebFetch``, ``curl`` via shell, a filesystem write tool, etc. We can't bind a specific tool name here because the right one varies per agent host (Claude / GPT / OpenClaw / raw HTTP client all differ).  Keeping the field structured (not just a description) defeats the \"scratchpad volatility\" failure mode where an agent forgets to act on the enriched-file URL after the metadata response satisfies the immediate ask.
    """ # noqa: E501
    action: Optional[StrictStr] = 'http_get_and_save'
    expires_in_seconds: StrictInt
    instruction: Optional[StrictStr] = 'HTTP GET the URL and save the response bytes to disk as ``save_as``. The file is the original upload with the extracted metadata embedded into its document properties (XMP for PDFs, EXIF/XMP for images) -- no client-side embedding needed. Skip only if the user explicitly asked for the metadata payload alone.'
    save_as: StrictStr
    url: StrictStr
    __properties: ClassVar[List[str]] = ["action", "expires_in_seconds", "instruction", "save_as", "url"]

    @field_validator('action')
    def action_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['http_get_and_save']):
            raise ValueError("must be one of enum values ('http_get_and_save')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of NextActionOutDTO from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of NextActionOutDTO from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "action": obj.get("action") if obj.get("action") is not None else 'http_get_and_save',
            "expires_in_seconds": obj.get("expires_in_seconds"),
            "instruction": obj.get("instruction") if obj.get("instruction") is not None else 'HTTP GET the URL and save the response bytes to disk as ``save_as``. The file is the original upload with the extracted metadata embedded into its document properties (XMP for PDFs, EXIF/XMP for images) -- no client-side embedding needed. Skip only if the user explicitly asked for the metadata payload alone.',
            "save_as": obj.get("save_as"),
            "url": obj.get("url")
        })
        return _obj


